# AI model configuration for code review
# Using OpenAI provider with OpenRouter base URL

models:
  gemini:
    type: gemini
    api_key: "{{env.GEMINI_API_KEY}}"
    model: gemini-3.5-flash
    timeout: 100000
